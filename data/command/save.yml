---
regex: ^(?:s|sa|sav|save)$
fun: |-
  if(player.mob instanceof Character){
    player.message(JSON.stringify(player.mob.createCharacterData()));
    console.log(player.mob.createCharacterData());
  } else {
    player.message("You're not even a character.");
  }