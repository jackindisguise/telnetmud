---
regex: (?:o|oo|ooc) (.+)
fun: |-2
  for(let target of MUD.players){
    if(target === player) target.sendMessage(`You: ${arguments[0]}`);
    else target.sendMessage(`${player.mob.name}: ${arguments[0]}`);
  }
