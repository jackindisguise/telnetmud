---
regex: ^(?:o|oo|ooc) (.+)
params: message:text
fun: |-
  for(let target of MUD.players){
    if(target === player) target.chat(_("You: %s", arguments[0]));
    else target.chat(_("%s: %s", player.mob.name, arguments[0]));
  }
