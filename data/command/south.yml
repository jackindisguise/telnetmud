---
regex: ^(?:s|so|sou|sout|south)\s*$
fun: |-
  let dir = direction.Direction.SOUTH;
  if(!player.mob.canStep(dir)) {
    player.message("Alas, you cannot go that way.");
  } else {
    player.mob.step(dir);
    player.showRoom();
  }