---
regex: ^(?:w|we|wes|west)$
fun: |-
  let dir = direction.Direction.WEST;
  if(!player.mob.canStep(dir)) {
    player.message("Alas, you cannot go that way.");
  } else {
    player.mob.step(dir);
    player.showRoom();
  }